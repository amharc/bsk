#ifndef _RUN_H
#define _RUN_H

#include <stdio.h>

/* Performs the taks from the problem statement.
 *
 * Reads lines from `in` and writes output to `out`.
 */
int run(FILE *in, FILE *out);

#endif /* !_RUN_H */
